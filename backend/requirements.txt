# --- Milestone 1: Project Foundation ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Milestone 2: Excel Upload ---
openpyxl==3.1.5

# Testing
pytest==8.3.3
httpx==0.27.2

# --- Milestone 8: Chart Engine ---
# Uses openpyxl.chart (native, editable Excel chart objects), already
# covered by the openpyxl dependency above -- no new package needed.
# matplotlib was considered but not used: it would render static images
# instead of live charts linked to the sheet's data.

# --- Added in later milestones (not used yet) ---
# pandas, xlsxwriter, numpy -> Workbook Reader / engine milestones

# --- Phase 3: AI (Gemini calls via httpx, see app/ai/) ---
# truststore makes Python's ssl module verify against the OS-native trust
# store instead of certifi's bundled list -- needed on machines where a
# corporate/security-software root CA is trusted by Windows but isn't in
# certifi's independent bundle (otherwise every outbound HTTPS call to the
# AI provider fails with CERTIFICATE_VERIFY_FAILED).
truststore==0.10.4

# --- Phase 4: Database + Auth ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3
bcrypt==4.2.0
pyjwt==2.9.0
email-validator==2.2.0
